dependencies:
    pre:
        - sudo apt-get install build-essential clang gcc-arm-none-eabi
test:
    pre:
        - make clean all CC=gcc 
        - make clean all CC=clang
        - make clean all BUILD_FOR_ARM=1
#    override:
#        - make check
